---
- name: VMs nacheinander sauber herunterfahren
  hosts: localhost
  gather_facts: false
  vars:
    # Die Liste der VMs aus dem AWX Survey
    #target_vms:
    #  - "Server_01"
    #  - "Server_02"

  tasks:
    - name: "Starte sequenziellen Shutdown-Prozess"
      include_tasks: shutdown_logic.yml
      loop: "{{ target_vms }}"
