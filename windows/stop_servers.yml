---
- name: Virtuelle Maschinen in vSphere herunterfahren
  hosts: localhost
  gather_facts: false
  vars:
    # Diese Variablen können auch als "Survey" in AWX abgefragt werden
    vm_name: "WEC01" 
    force_poweroff: false  # Wenn true, wird der "Stecker gezogen" (Hard Stop)

  tasks:
    - name: "Fahre VM {{ vm_name }} kontrolliert herunter"
      community.vmware.vmware_guest_powerstate:
        hostname: "{{ lookup('env', 'VMWARE_HOST') }}"
        username: "{{ lookup('env', 'VMWARE_USER') }}"
        password: "{{ lookup('env', 'VMWARE_PASSWORD') }}"
        validate_certs: false
        name: "{{ vm_name }}"
        state: shutdown-guest  # Versucht einen sauberen OS-Shutdown via VMware Tools
      register: shutdown_result
      ignore_errors: yes

    - name: "Erzwinge Ausschalten, falls Gast-Shutdown fehlschlägt"
      community.vmware.vmware_guest_powerstate:
        hostname: "{{ lookup('env', 'VMWARE_HOST') }}"
        username: "{{ lookup('env', 'VMWARE_USER') }}"
        password: "{{ lookup('env', 'VMWARE_PASSWORD') }}"
        validate_certs: false
        name: "{{ vm_name }}"
        state: poweredoff
      when: 
        - shutdown_result.failed
        - force_poweroff | bool
